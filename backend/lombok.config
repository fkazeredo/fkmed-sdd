# DECISIONS-BASELINE §0013 — Lombok for boilerplate only.
config.stopBubbling = true
# Lets JaCoCo ignore Lombok-generated code in coverage.
lombok.addLombokGeneratedAnnotation = true
# Standard JavaBean accessors (baseline §0013: fluent accessors off for entities).
lombok.accessors.fluent = false
