/**
 * Domain error kernel (DECISIONS-BASELINE §0012): the pure {@code DomainException} base every
 * business module builds its specific exceptions on.
 */
@org.springframework.modulith.ApplicationModule(displayName = "error kernel")
package com.fkmed.domain.error;
