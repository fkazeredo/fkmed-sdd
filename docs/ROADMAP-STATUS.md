# ROADMAP-STATUS — execution log

Append-only execution log: **one line per closed slice**, written by `/dod`. Canonical
source order for status reports: this file → `release-notes/CHANGELOG.md` →
`decision-log/INDEX.md` → `git log` → `ROADMAP.md`. PR open ≠ merged ≠ released with tag —
never conflate (DECISIONS-BASELINE §0023).

| Date | Phase | Slice | Spec(s) | Branch / PR | Gates | E2E | Notes |
|---|---|---|---|---|---|---|---|
| 2026-07-04 | 0 | walking-skeleton | 0001 | `feature/walking-skeleton` / PR #2 (merged) | ✅ backend verify (83 tests, PIT 98%) · frontend lint/test/build (13 tests) — run 3× (dev, QA, /dod) | ✅ 3/3 isolated stack | v0.1.0 (tag pending, owner). QA APPROVED zero blockers; fresh-eyes rework (11 items) done. CI-triage: `mvnw` exec bit fixed (Windows-dropped, 4 jobs). Owner decisions: tenant_id removed → ADR-0003 (revises §0003, single-tenant per build); CSRF false-positive → ADR-0002 (owner to dismiss CodeQL alert). ADR-0001/0002/0003 Proposed |
| 2026-07-04 | 1 | 1.1 first-access-login | 0002, 0003 (subset) | `feature/first-access-login` / PR pending | ✅ backend verify (167 tests, PIT 90%/96%) · frontend lint/test/build (32 tests) — run by dev, QA (independent, APPROVED) and architect (re-verified post-rework) | ✅ 4/4 isolated stack (+ Mailpit) | v0.2.0. Real accounts replace the SPEC-0001 dev-login seam (MARIA seeded ACTIVE, PEDRO exercises first access). Fresh-eyes: approve with reservations → 14-item rework (1 spec gap: CPF check-digit; rest test-quality gaps) all closed with committed regressions. ADR-0004 (Mailpit email seam), DL-0001 (first-access contract shape). Slice covers 0002's first-access/verification/login BRs + 0003's audit-trail BR6/BR7/BR10 only — lockout/recovery/selector/protocol-generator are slices 1.2/1.3 |
| 2026-07-04 | 1 | 1.2 account-security | 0002 (BR8–BR12) | `feature/account-security` / PR #9 (merged) | ✅ backend verify · frontend lint/test/build (green at merge) | ✅ account-security journeys | v0.3.0 (**recorded retroactively in slice 1.3** — 1.2 merged without the `/dod` version/changelog/status/report bookkeeping; reconciled here). Lockout (15 min after 5 fails), recovery (30-min single-use link, terminates other sessions), authenticated password change, session model (30 min idle / 7 days), Segurança screen. DL-0002/DL-0003. |
| 2026-07-04 | 1 | 1.3 beneficiary-context | 0003 (BR1–5, BR8) + 0002 debts | `feature/beneficiary-context` / PR pending | ✅ backend verify (102 cls) · PIT 87% killed / 96% strength · frontend lint/test/build (81) | ✅ 7/7 isolated stack | v0.4.0. Accessible-beneficiaries + scope-checked card endpoint (404 not revealing existence — BR2), header selector; débito A `@Version`+retry+409 (`ConcurrentFailedLoginIT` red→green); débito B disposable E2E account (MARIA never mutated). DL-0004/DL-0005. **Orchestration incident (owner-visible):** dev-backend wrote into the MAIN worktree instead of its own (absolute-path pitfall) — caught by a status check before any bad commit, work rescued to `--be`, backend context finished inline; safeguard saved to the team workflow (PR #11). |
| 2026-07-05 | 1 | 1.4 home | 0005 | `feature/home` / PR pending | ✅ backend verify (234) · PIT 83% killed / 96% strength · frontend lint/test/build (101) | ✅ 8/8 isolated stack | v0.5.0 — **closes Phase 1**. Home: beneficiary card reacting to the active-beneficiary switch (headline journey), Acesso Rápido (undelivered "em breve"), banners (6 s rotation/pause), notices (single-open); new `domain.content` module + `GET /api/content/home` (V8, ADR-0006); post-login landing → Home. Parallel BE×FE with the worktree-pin safeguard active — **landing check confirmed both agents in their own worktrees, main clean (the 1.3 incident did NOT recur)**. Integration finding: 1 E2E banner assertion (circular-carousel clone) fixed. SPEC-0005 approved with a phased-delivery note (AC2/AC6 → Phases 2/5). |
| 2026-07-05 | 3 | phase-3 (0008+0009) | 0008, 0009 | `feature/phase-3` / PR pending | ✅ backend verify (413 tests, incl. real 2-thread slot-race IT) · frontend lint/test/build (349, 92% stmts) — run by devs + architect (integrated, re-verified after the lockstep bump) | ⏳ CI Playwright (funil localidade→especialidade, busca por nome/rota, consulta completa, exame bloqueado sem anexo, cancelar/remarcar) — monitored to green on push | v0.7.0 — **closes Phase 3**. Provider network search (SPEC-0008): read-only `domain.network` (8th module, ADR-0011) with a full IBGE geography registry (27 UFs + ~5,570 municipalities, V15), funnel + name search scoped to `plan.coverage`/`plan.coverage_uf` (DL-0014, fail-closed), provider detail with seals (DL-0012). Appointments (SPEC-0009): transactional `domain.appointment` (9th module, ADR-0012, V16) with **real slot capacity under concurrency** (optimistic `@Version`, fail-fast no-retry — exactly one of two racers wins, proven by `AppointmentConcurrencyIT`), state-machine status (AGENDADO→REAGENDADO\|CANCELADO\|REALIZADO, REALIZADO derived on read), unique `AG-` protocol from a shared generator in `domain.plan` (DL-0016), medical-order upload (magic-byte + `%PDF`, DL-0015), 2 h antecedence (DL-0013) / 30-day horizon. Cross-spec: `AppointmentConfirmed/Cancelled/Rescheduled` → `AppointmentNotificationListener` in `domain.notification` (AFTER_COMMIT + REQUIRES_NEW, no PII), V17 seeds `appointment.cancelled/.rescheduled`; specialty registry shared `appointment → network` (no cycle). **4 parallel dev agents in 2 waves (Wave-1 network BE×FE, Wave-2 appointments BE×FE off the integrated Wave-1 tip)**; governance committed BEFORE branching (no numbering collision, unlike Phase 2). Contract re-syncs at integration: bare-array network responses, `medicalOrder` upload part, `{upcoming,history}` list shape, `/exams` catalog. Fresh-eyes APPROVED (2 minor non-blocking notes: `outside-horizon` reused for invalid-code/missing-agenda edge; provider `detail` not coverage-scoped — directory data, BR13 satisfied). DL-0012..0016, ADR-0011/0012. |
| 2026-07-05 | 2 | phase-2 (0004+0006+0007) | 0004, 0006, 0007 | `feature/phase-2` / PR pending | ✅ backend verify (321 tests) · frontend lint/test/build (204) — run by devs + architect (integrated, re-verified) | ✅ 15/15 Playwright on the isolated stack (CI green) | v0.6.0 — **closes Phase 2**. Notification center + bell (SPEC-0004); profile: photo/contacts/versioned legal + interception/sign out (SPEC-0006); digital card + PDF (SPEC-0007). New modules `domain.notification` (V10, ADR-0008) + `domain.card` (V9, ADR-0007/0010); profile extends plan/identity (V11/V12). Cross-spec: `ContactDataChanged` → mandatory `account.contact-changed` (in-app + e-mail to old & new). Extras (owner): **CodeQL JWT de-sensitization** (card number out of the token, ADR-0009, regression `LoginAndTokenFlowIT` red→green) + **`AuditRetentionJob` infra→application**. **7 parallel dev agents in 2 waves (BE×FE)**; recovered from a transient API session-limit mid-wave-2 with zero work lost (resumed via SendMessage). Integration re-syncs: notif preferences envelope, profile legal-body/accept-version/avatar-blob-auth. Fixed a flaky module-diagram gate (deterministic `Rel` ordering). DL-0006..0011, ADR-0007..0010. **CI-green fix round after PR open:** CodeQL ReDoS (email regex → linear + length guard), V14 seeds the V7 E2E account's terms acceptance (SPEC-0006 interception was blocking every pre-existing unaccepted account — a Phase-1 E2E regression), and the E2E caught a **real FE bug** (null optional-contact fields → `null.trim()`, fixed + regression). All **15 CI checks green** incl. PIT mutation + Playwright 15/15. |
